import Ember from 'ember';
import layout from '../../templates/components/paper-swiper/slide-media';

export default Ember.Component.extend({
  layout,
  classNames: ['flex', 'paper-swiper__slide-media', 'layout-column', 'layout-align-center-center']
});
